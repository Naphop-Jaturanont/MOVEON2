using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Video;

public class VideoCheckpoint : MonoBehaviour
{
    private int IndexVideo;
    private VideoClip videoClip;
    public int indexVideo { get { return IndexVideo; } set { IndexVideo = value; } }
    public VideoClip video { get { return videoClip; } set { videoClip = value; } }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
